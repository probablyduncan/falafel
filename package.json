{
  "name": "falafel",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "updatePublicationRecord": "tsx --env-file=.env src/lib/standardSitePublisher.ts",
    "deleteDocumentRecords": "tsx --env-file=.env src/lib/atprotoDocumentDebugHelper.ts",
    "falafel": "tsx --env-file=.env scripts/fetchFalafel.ts",
    "update-lexicons": "pnpm exec lex install --update --save",
    "postinstall": "pnpm exec lex install --ci",
    "prebuild": "pnpm exec lex build && tsx --env-file=.env scripts/fetchFalafel.ts"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@googlemaps/places": "^2.5.0",
    "@atproto/lex-password-session": "^0.1.4",
    "@atproto/lex": "^0.1.7",
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}